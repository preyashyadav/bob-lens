// Node.js execution template for Bob Lens Sandbox

// Test inputs are available in the testInputs variable
// User code will be executed below

// {{USER_CODE}}

// Export results if needed
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { testInputs };
}

// Made with Bob
